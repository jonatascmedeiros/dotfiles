disable mouse acceleration

install updates
	nvidia driver

config monitor shutdown/lock screen

install dropbox		
	vlc
	netflix (ppa:pipelight/stable)
		sudo apt-get install --install-recommends pipelight-multi
		sudo pipelight-plugin --update
		sudo pipelight-plugin --enable silverlight
		install User Agent Overrider on firefox
	teamviewer

fix vsync on xfce
	switch off compositing in Window Manager Tweaks
	install compton (ppa:richardgv/compton)
	put file compton.conf (from dropbox) in ~/.config	
	set Application Autostart in Session and Startup
		command: compton

install	libglew-dev
	git
	git-gui
	qt sdk 		
		set font DejaVu Sans Mono (12)
		glsl.xml -> ~/.config/QtProject/qtcreator/genereic-highlighter
		go to tools->Environment->MIME-Types and delete file extensions for application/x-glsl
		VS for QT Creator.xml -> ~/.config/QtProject/qtcreator/styles (create folder)	
		import qt2.xml (Options->C++) 	

set up bluetooth
	add to /etc/rc.local
		modprobe btusb
		echo "0b05 17cb" >> /sys/bus/usb/drivers/btusb/new_id	

xbox controller
	xpad should work out of the box, if not, take a look at xboxdrv

webcam (untested)
	see if it works :)
